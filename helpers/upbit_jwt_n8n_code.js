// Deprecated placeholder.
// Upbit auth signing has moved out of n8n Code nodes into the FastAPI
// service under `upbit-helper/`. WF03 now calls helper HTTP telemetry
// endpoints only and does not perform auth/signing in n8n.
